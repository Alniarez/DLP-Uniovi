/*
Este ejemplo tiene varios errores semánticos que el compilador debe detectar en la
fase de identificación y otros que no deben ser detectados aún.

NOTA: Es posible que el alumno deba adaptar ligeramente la sintaxis del lenguaje si no coincide
con la del lenguaje que le corresponde.
*/

var a:int;
var b:char;
var d:float;
var e:float;

var a:[20] int;	// Variable global repetida SI

// ------------- 
struct Persona {
	edad:int;
	meses:[12] int;
	edad:float;  // Campo repetido NO
};

var k:[10] Window;	// Estructura no definida SI

struct Persona {	// Estructura repetida SI
	j:int;
};

// ------------- 
dobleSuma(z:int, b:int, z:float): int { // Parámetro 'z' repetido SI ('b' no colisiona con global SI) 
	var local1:int;
	var local2:int;		
	var local1:int;		// Local repetida (con otra local) SI
	var b:float;		// Local repetida (con un parámetro) SI
	var d:int;		// Esta no debía dar error, ya que la otra 'd' es global 
	
	print a;	
	print d;
	print b;	
	print c;		// variable no definida SI
	print local2;	
}

struct Punto {
	x:int;
};

var centro:Punto;

main() {
	print f();	// Función no definida  SI
	g(2);		// Procedimiento no definido SI 
	
	dobleSuma(5.6);	// No error en esta fase aunque no coincidan parámetros 
	print centro.y; // No error en esta fase aunque aunque el campo no está en la struct 
}

dobleSuma() {	// Función repetida SI

}